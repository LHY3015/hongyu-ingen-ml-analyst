Hongyu LIU
InGen Dynamics - ML & NN Analyst Intern, June 2026

---

## 1. Dataset Overview

A synthetic Aido Rover patrol dataset was generated by a world core to mirror realistic deployment. The dataset captures 15,000 timesteps (25 minutes of continuous operation) across nine sensor channels.

| Property            | Value                                                                        |
| ------------------- | ---------------------------------------------------------------------------- |
| Total samples       | 15,000                                                                       |
| Sampling rate       | 10 Hz                                                                        |
| Duration            | 25.0 min                                                                     |
| Sensor channels     | 9 (gps_lat, gps_lon, lidar_distance, battery_soc, torque_0–3, ambient_temp) |
| Anomaly class       | 15.1% (2,258 samples)                                                        |
| Normal class        | 84.9% (12,742 samples)                                                       |
| Anomaly composition | 1,369 slip-type + 889 stuck-type samples                                     |

**Anomaly generation logic:** Anomalies are a causal mechanical self-fault, not an injected burst. Two fault mechanisms share one binary label: **slip** (one wheel's torque spikes while the other three shed load, so displacement is reduced by `v·(1−0.7·s)`) and **stuck** (all four wheel torques rise together and displacement collapses to `v·(1−s)`). A latent severity `s ~ Beta(2,5)` scales the deviation amplitude for every event, and any active fault is labeled 1 regardless of severity — weak events sink into the noise floor, which is what makes the detection task non-trivial. Torque and battery both respond to the same terrain-driven demand, so the fault↔signal correlation is statistical rather than hardcoded (rough terrain alone also raises torque and drains the battery faster), avoiding label leakage into the feature set.

## 2. Data Quality & Cleaning

0.5% random missingness was injected across the nine sensor channels (simulating random sensor data missing in real world). Forward-fill then backward-fill was applied; the label column was not imputed.

| Metric                         | Value         |
| ------------------------------ | ------------- |
| Missing values before cleaning | 718 (0.53%)   |
| Missing values after cleaning  | 0             |
| Duplicate rows                 | 0             |
| Timestamp gaps                 | 0             |
| Torque outliers (>3 std)       | 0 per channel |
| LiDAR outliers (>3 std)        | 298           |

Only `lidar_distance` produces 3σ outliers, and they correspond to the LiDAR's 200 m no-return dropout spikes (`DROPOUT_P=0.02` in the world core), not to faults. The four torque channels show **zero** 3σ outliers: terrain already spreads torque over a wide, bimodal range (asphalt/gravel low vs. wet-grass/mud high), so a slip/stuck event's added deviation is usually absorbed inside that natural spread rather than pushing past 3 standard deviations. This is the expected consequence of the statistical (not deterministic) fault↔terrain coupling, and it is why a simple 3σ threshold cannot separate faults from rough-terrain driving — the FFT/PCA/RF pipeline below is needed instead.

## 3. FFT Feature Extraction

To capture the spectral signature of torque anomalies (sustained high-frequency energy during bursts vs. near-DC content during normal patrol), FFT features were computed over a 50-step sliding window (5 s at 10 Hz) for five channels.

**Channels transformed:** `torque_0`, `torque_1`, `torque_2`, `torque_3`, `lidar_distance`

**Features per channel (5):**

| Feature          | Description                                              |
| ---------------- | -------------------------------------------------------- |
| `dom_freq`     | Frequency of maximum spectral magnitude (Hz)             |
| `centroid`     | Spectral centroid — energy-weighted mean frequency (Hz) |
| `bandwidth`    | Spectral bandwidth — spread around centroid (Hz)        |
| `total_power`  | Sum of squared magnitudes (energy proxy)                 |
| `peak_to_mean` | Ratio of peak to mean spectral magnitude (peakedness)    |

This yields 25 FFT features (5 channels × 5 features), added to the 9 raw sensor values for a 34-dimensional feature matrix covering 14,950 windowed samples (rows 50–14,999 of the cleaned trace).

**Design rationale:** The 50-step window matches the state look-back in the MDP schema, so FFT features and RL state features are computed over identical temporal contexts. This allows direct comparison between classical and RL representations in later phases.

## 4. Train / Val / Test Split

Stratified random splitting was applied to the 34-feature matrix (14,950 samples).

| Split       | Samples | Normal | Anomaly | Anomaly % |
| ----------- | ------- | ------ | ------- | --------- |
| Train (70%) | 10,465  | 8,884  | 1,581   | 15.1%     |
| Val (15%)   | 2,242   | 1,904  | 338     | 15.1%     |
| Test (15%)  | 2,243   | 1,904  | 339     | 15.1%     |

## 5. PCA

PCA with 95% variance retention was applied to the 34-feature scaled matrix.

| Property            | Value  |
| ------------------- | ------ |
| Input dimensions    | 34     |
| Retained components | 17     |
| Variance explained  | 96.04% |

Variance per component (first 10):

| Component | Variance | Cumulative |
| --------- | -------- | ---------- |
| PC1       | 34.04%   | 34.04%     |
| PC2       | 11.05%   | 45.09%     |
| PC3       | 8.94%    | 54.03%     |
| PC4       | 6.25%    | 60.28%     |
| PC5       | 5.58%    | 65.87%     |
| PC6–10   | 17.06%   | 82.94%     |
| PC11–17  | 13.09%   | 96.04%     |

![pca_scree](image/W02_Feature_Analysis_Report/pca_scree_0.png)

PC1 alone now carries 34.0% of the variance — noticeably more concentrated than a scheme with several independently-randomized burst parameters, because the causal fault model routes both mechanisms (slip and stuck) through one shared physical driver: terrain-modulated wheel torque. Components PC6–17 capture residual sensor correlations (GPS random walk, temperature seasonality, LiDAR range noise) that contribute little discriminative power.

**Principal component interpretation (top-5 loadings):**

| PC  | Top contributors                                                              |
| --- | ----------------------------------------------------------------------------- |
| PC1 | torque_0–3 spectral centroid, torque_2 peak-to-mean                          |
| PC2 | torque_0–3 raw amplitude, torque_0 bandwidth                                 |
| PC3 | lidar_distance centroid, peak-to-mean, total_power, dom_freq, bandwidth       |
| PC4 | torque_0–3 spectral bandwidth, torque_2 total_power                          |
| PC5 | torque_3 total_power, torque_0 bandwidth/total_power, torque_1–2 total_power |

## 6. RF Feature Selection

A baseline Random Forest (100 trees, unlimited depth) was trained on all 17 PCA components to compute feature importances. Accuracy was then measured for top-K subsets (K = 1 … 17) to find the minimal K within 2 pp of full-set validation accuracy.

**Full-set baseline:**  96.21% val accuracy (17 PCA components)
**2 pp threshold:**  94.21%
**Minimal top-K selected:**  K = 4
**Top-4 val accuracy:**  94.25% (delta = 1.96 pp)

![rf_feature_selection](image/W02_Feature_Analysis_Report/rf_feature_selection_0.png)

The RF selects the top-4 PCA components — PC2 (raw torque amplitude), PC1 (torque spectral centroid), PC3 (LiDAR spectral shape), and PC4 (torque spectral bandwidth) — as the minimal sufficient set.

**Comparison to CNC methodology:** In the prior CNC wear-detection pipeline (18-channel multi-sensor data, FFT/PCA/RF cascade), the RF selected exactly 2 features (Z1 positional channels) from a raw feature space, achieving 97.81% test accuracy. The Rover pipeline requires 4 PCA components to achieve 94.25% val accuracy — a wider feature footprint reflecting the subtler and multi-channel nature of a mechanical self-fault compared to the CNC positional wear signature.

## 7. RF Benchmark Results

Grid search over `n_estimators ∈ {50, 100, 200}` × `max_depth ∈ {None, 5, 10, 20}` (12 combinations, 5-fold cross-validation, F1 scoring, `class_weight='balanced'`).

**Best hyperparameters:** `n_estimators=200, max_depth=None`  **CV best F1:** 0.7582

**Train Metrics**

| Class    | Precision | Recall | F1     |
| -------- | --------- | ------ | ------ |
| Normal   | 1.0000    | 0.9971 | 0.9985 |
| Anomaly  | 0.9838    | 1.0000 | 0.9918 |
| Accuracy |           |        | 0.9975 |

AUC-ROC (train): 1.0000

**Validation Metrics**

| Class    | Precision | Recall | F1     |
| -------- | --------- | ------ | ------ |
| Normal   | 0.9770    | 0.9386 | 0.9574 |
| Anomaly  | 0.7167    | 0.8757 | 0.7883 |
| Accuracy |           |        | 0.9291 |

AUC-ROC (val): 0.9707

**Test Metrics**

| Class    | Precision | Recall | F1     |
| -------- | --------- | ------ | ------ |
| Normal   | 0.9729    | 0.9428 | 0.9576 |
| Anomaly  | 0.7261    | 0.8525 | 0.7843 |
| Accuracy |           |        | 0.9291 |

AUC-ROC (test): 0.9742

![rf_benchmark](image/W02_Feature_Analysis_Report/rf_benchmark_train_val_test.png)

The model achieves high anomaly recall (85.3% test) with moderate precision (72.6% test), consistent with a class-weighted classifier prioritising detection over false-alarm reduction.

## 8. Overfitting Analysis

`max_depth=None` was selected by the grid search, so individual trees are grown to pure leaves. The **train** set (predicted by the same `best_rf`) is included above alongside validation and test to check whether this translates into harmful overfitting.

| Split                    | Anomaly F1 | AUC-ROC |
| ------------------------ | ---------- | ------- |
| CV (5-fold, out-of-fold) | 0.7582     | —      |
| Train                    | 0.9918     | 1.0000  |
| Validation               | 0.7883     | 0.9707  |
| Test                     | 0.7843     | 0.9742  |

**Train reaching near-perfect fit is expected, not itself a red flag.** With unlimited depth, a random forest's trees can memorize the training set almost exactly — this is normal RF behaviour, and by itself says nothing about generalization.

**The diagnostic that matters is whether validation/test degrade relative to the CV estimate, and they do not.** `gs.best_score_` (0.7582) is the average F1 across 5 held-out folds during hyperparameter search — a genuine out-of-sample estimate, and one that is typically *optimistic* because it is also the criterion used to pick the winning hyperparameters among 12 candidates. If the selected model were overfitting, validation and test (fully independent of training and hyperparameter selection) would score **below** this CV estimate. Instead they are equal to or slightly above it (0.7883, 0.7843 vs. 0.7582), and validation and test agree closely with each other (F1 within 0.004, AUC within 0.0035, accuracy identical at 0.9291). The slightly higher val/test score is consistent with `best_rf` being refit on the *full* training set (10,465 rows) versus the ~80% per fold used during CV — more training data, not less generalization.

**Why the train/val gap doesn't hurt generalization here:**

- Bagging + feature subsampling across 200 trees averages out each tree's memorized noise; the ensemble's decision boundary is smoother than any individual tree's.
- The classifier operates on 4 PCA components distilled from 34 raw+FFT features, which limits the degrees of freedom available to fit incidental noise in the first place.
- §2 showed the fault signal statistically overlaps rough-terrain torque rather than sitting in a hardcoded band — there is no deterministic shortcut for a tree to memorize.

**Conclusion:** the train/val/test pattern is consistent with a well-generalizing ensemble, not a memorized one. The relevant number for deployment is the test set (F1 = 0.7843, AUC = 0.9742), not the train score.

## 9. Latency & Platform Feasibility

Measured with `timeit`, mean over 100 repetitions. Aido Rover real-time constraint: **≤ 100 ms** per sample at 10 Hz streaming.

| Criterion                              | Result         |
| -------------------------------------- | -------------- |
| Test F1 (anomaly class)                | 0.7843         |
| Test AUC-ROC                           | 0.9742         |
| Single-sample Inference latency        | 7.35 ms        |
| 1,000-sample batch total               | 19.24 ms       |
| Latency constraint (Aido Rover, 10 Hz) | ≤ 100 ms      |
| Deployment verdict                     | **PASS** |

The FFT-PCA-RF pipeline is feasible for real-time Aido Rover deployment. The classifier trained on 4 PCA components achieves F1=0.784 on the anomaly class with AUC-ROC=0.974 while running at approximately 7.4 ms per decision cycle (200 trees, unlimited depth) — comfortably within the 100 ms streaming budget, with headroom to spare for the upstream FFT window computation and PCA transform in the production path.
